# 团队编码规范

> 星链科技研发团队 · 版本 1.2 · 2026-03-01

---

## 一、命名规范

### 1.1 变量与函数

- 变量和函数名使用 **snake_case** 风格，如 `user_name`、`get_total_count()`
- 布尔类型变量以 `is_`、`has_`、`can_` 开头，如 `is_active`、`has_permission`
- 私有方法以单下划线开头，如 `_parse_document()`
- 魔术方法使用双下划线包裹，如 `__init__`、`__str__`

### 1.2 类与模块

- 类名使用 **PascalCase** 风格，如 `KnowledgeBaseService`、`DocumentParser`
- 模块名使用小写字母加下划线，如 `document_service.py`
- 异常类以 `Error` 或 `Exception` 结尾，如 `DocumentParseError`

### 1.3 常量

- 常量使用全大写加下划线，如 `MAX_RETRY_COUNT = 3`
- 模块级常量集中定义在文件顶部或独立的 `constants.py` 中

---

## 二、注释规范

### 2.1 函数文档

- 所有公开函数必须编写 docstring，采用 **Google 风格**
- docstring 须包含：功能描述、参数说明（Args）、返回值说明（Returns）、异常说明（Raises）

```python
def parse_document(file_path: str, file_type: str) -> list[dict]:
    """解析文档文件并返回切块列表。

    Args:
        file_path: 文档文件的绝对路径。
        file_type: 文件类型，支持 pdf/docx/md/txt。

    Returns:
        切块字典列表，每个字典包含 text 和 index 字段。

    Raises:
        FileNotFoundError: 文件路径不存在时抛出。
        UnsupportedFileTypeError: 不支持的文件类型时抛出。
    """
```

### 2.2 行内注释

- 行内注释使用 `#` 开头，与代码之间保留两个空格
- 注释应解释"为什么"而非"做了什么"
- 禁止提交含 `TODO`、`FIXME` 注释的代码到 `main` 分支

---

## 三、Git 规范

### 3.1 分支命名

- 功能分支：`feature/模块名-简要描述`，如 `feature/document-upload`
- 修复分支：`fix/issue编号-简要描述`，如 `fix/issue-42-chunk-index`
- 发布分支：`release/版本号`，如 `release/v1.2.0`

### 3.2 Commit 消息

采用 **Conventional Commits** 规范：

- `feat`: 新功能，如 `feat: 支持 PDF 文档解析`
- `fix`: 修复缺陷，如 `fix: 修复切块索引越界问题`
- `docs`: 文档变更，如 `docs: 更新 API 接口说明`
- `refactor`: 重构，如 `refactor: 提取文档解析服务`
- `test`: 测试相关，如 `test: 补充向量检索单测用例`

### 3.3 Code Review

- 所有合并到 `main` 的 PR 必须经过至少 1 名 Tech Lead 审核
- PR 描述须包含：变更目的、影响范围、测试方式
- 单次 PR 变更文件数不超过 15 个，超出应拆分为多个 PR

---

## 四、Python 项目规范

### 4.1 依赖管理

- 使用 `pyproject.toml` 管理项目依赖，禁止直接修改 `requirements.txt`
- 生产依赖与开发依赖分组管理
- 锁定依赖版本使用 `poetry.lock` 或 `uv.lock`

### 4.2 代码质量

- 所有代码必须通过 `ruff` 检查，零警告合并
- 类型注解覆盖率不低于 80%，使用 `mypy --strict` 校验
- 单元测试覆盖率不低于 70%
